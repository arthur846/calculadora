import { StyleSheet } from "react-native";

const stylekey = StyleSheet.create ({
    container:{
        alignItems: 'center',
        width:'90%',
    },
    subContainer:{
        flexDirection: 'row'
    },    
})

export default stylekey;